{
  "name": "snip-backend",
  "version": "1.0.0",
  "description": "Tiny URL shortener API — single-file Bun server, zero npm dependencies.",
  "type": "module",
  "scripts": {
    "start": "bun run server.js"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
